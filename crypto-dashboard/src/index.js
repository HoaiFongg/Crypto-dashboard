import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CryptoGrid from './App.jsx';  // Cập nhật đường dẫn

ReactDOM.render(
  <React.StrictMode>
    <CryptoGrid />
  </React.StrictMode>,
  document.getElementById('root')
);
